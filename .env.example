# Optional GitHub PAT to bump API rate limits at build time.
# Scopes: public_repo, read:user.
# Without this, the build uses unauthenticated requests (60/hr).
GITHUB_TOKEN=

# Optional: short SHA injected into the build footer.
GIT_SHA=
